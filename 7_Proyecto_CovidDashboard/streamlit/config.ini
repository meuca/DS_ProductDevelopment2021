[mysql]
host = db
port = 3306
database = dash
user = admin
password = admin12345

[queries]
pullDeaths = SELECT * FROM dash.deaths
pullConfirmed = SELECT * FROM dash.confirmed
pullRecovered = SELECT * FROM dash.recovered